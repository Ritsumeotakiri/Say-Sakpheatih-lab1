services:
  - type: web
    name: say-sakpheatih-lab1
    env: node
    buildCommand: yarn install
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production